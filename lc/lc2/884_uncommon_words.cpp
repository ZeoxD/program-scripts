#include<iostream>
#include<string>
#include<vector>

using namespace std;

class Solution {
public:
    vector<string> uncommonFromSentences(string s1, string s2) {
        int len1 = s1.length();
        int len2 = s2.length();

        for(int i = 0; i < len1; i++) {
            
        }
    }
};